const tipCollection = ["Maintain Balance", "Decorate for the fish", "Don't worry if the fish hide", "Lighting should be used to create a day and night cycle", "Clean regularly"]

export const getTips = () => {
    return tipCollection
}